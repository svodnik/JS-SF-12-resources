/*
DOM manipulation: Making a favorites list

Write code to enhance the favorites list in the following ways:
- When a user clicks the submit button, take the value they've typed in the input box and add it to the list (remember: appendChild)
- Also, when a new item is added to the list, clear the input box.  
*/

// STEP 1
// Create a variable that references the submit button element in the DOM.



// STEP 2
// Add an event listener to the element you selected in the previous step. Your event listener should listen for the click event, and should do the following:
// - create a new list item
// - create a new text node with a value equal to the value the user typed in the input box (use the `value` property of the input box element)
// - append the new text node to the list item element
// - append the new list item to the unordered list in the DOM
// - clear the input box   (Hint: the `value` property of the input box, before anyone types in it, is an empty string.)



// STEP 3
// Save your changes, open index.html in your browser, and test your code.

/*

BONUS:
Add functionality that adds a message to the page that alerts the user when they click Submit without typing anything. (Use DOM manipulation, not the alert method.)
Hint: Check the HTML file for a hidden element, and examine the classes defined in the CSS style sheet.

*/




