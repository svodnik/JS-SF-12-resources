const countDown = function() {
    for(let counter = 3; counter > 0; counter--) {
        console.log(counter);
    }
}; 

/*
function countDown() { 
    for(let counter = 3; counter > 0; counter--) {
        console.log(counter);
    }
} 
*/
